# v144.245 — Admin scheduling + Free-trial sidebar cleanup

## Changes

- The existing manual campaign scheduling bypass is now available to every verified Spreelo administrator, not only the hard-coded primary tester account.
- The original `johan@foldern.com` fallback remains in place for regression safety.
- Customer scheduling restrictions are unchanged.
- After an eligible social channel is connected, the sidebar performs a one-shot refresh of the authoritative credit/trial state if the client still sees the Free trial as locked.
- The “Unlock by connecting an eligible social channel” helper is never shown while a social channel is already connected.

## Database

No SQL migration is required for v144.245.
