# PATCH v144.195

## Home / Innehållsplaner — mobile
- Forces all three plan sections to use the exact same mobile header structure.
- Keeps `Återkommande scheman` as the visual reference.
- Aligns `Planerade inlägg` and `Kalenderkampanjer` to the same icon, title, status, description and CTA positions.
- Neutralizes older grid rules that could push the lower cards inward/right.

## Workspace loader — mobile
- Explicitly centers the whole loader card horizontally in the viewport.
- Keeps equal left/right margins in mobile Safari.

## Onboarding planned-post labels
- Adds horizontal padding inside colored content-type labels.
- Vertically centers the label text.
- Makes the mobile preview cards slightly wider so longer labels do not sit edge-to-edge.

## Translation / source-language check
- Static UI copy in the Variant 5 onboarding is referenced through `t(...)` translation keys.
- The canonical/default labels for the Variant 5 onboarding keys are English in `lib/i18n/defaultLabels.js`.
- Dynamic values such as the brand name, dates, connected channels and content-type data are intentionally data-driven rather than hard-coded UI strings.

No SQL changes.
