# Spreelo v144.161 — Automation prerender hotfix

## Fix
- Removes an unused `plannerUiCopy.spreeloChoosesLanguage` initializer that read the React `language` state before that state was initialized during `/automation` prerendering.
- Fixes Vercel production build error: `ReferenceError: Cannot access '<minified variable>' before initialization` on `/automation`.

## Scope
- No change to content-language selection behavior introduced in v144.160.
- No change to manual-prompt language detection.
- No change to product-title safety.
- No change to Social Channels or OAuth logic.
- No database migration required.
