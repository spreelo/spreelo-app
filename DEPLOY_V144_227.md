# Deploy v144.227

1. No SQL is required if `spreelo-v144.225-SQL.sql` has already been applied.
2. Deploy the app normally.
3. Open `/grow-brain?demo=1` while the isolated Step 3A test data is still present.
4. The new “What actually performs” section should show the persisted positive and negative Step 3A signals.
5. The section is read-only/observational. Step 4 planning influence remains disabled.
