# Deploy v144.245

1. Deploy the v144.245 application package to Vercel.
2. No Supabase SQL is required.
3. Add/accept `contact@spreelo.com` as a Spreelo administrator through the existing admin-team flow if it is not already an admin.
4. Sign out/in on that account if necessary so the administrator metadata/session is refreshed.
5. Verify that the scheduling UI now permits past dates/times for that admin account.
6. Verify that normal customer accounts still cannot schedule in the past.
7. Verify that a Free account with a connected social channel no longer shows the sidebar unlock instruction.
