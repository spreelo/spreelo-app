# Deploy v139 – calendar active row and title-fit polish

This version is a presentation-only update for the campaign calendar.

## What changed
- made the active/open campaign card slightly darker so the selected card is easier to see
- kept the left visual strip at a stable desktop-sized width on mobile too
- extended the tint underneath the left strip so future images can sit at the top while the color continues below
- gave campaign titles more room and allowed cleaner wrapping so the full title is easier to read

## Important
- no AI calls changed
- no campaign generation logic changed
- no credits logic changed
- no database migrations are required

## Deploy
Deploy the app as usual on Vercel.
