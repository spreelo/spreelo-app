# Deploy v144.223

This patch refines the Grow Brain performance chart:
- removes the misleading post-count bars,
- introduces two visible curves (interactions + exposure),
- and tightens the chart height for a closer match to the approved design direction.

No database or environment changes required.
