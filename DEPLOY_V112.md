# Deploy V112 — exact magic background under review glass

## Fixed
- `Granska och godkänn` now uses the **same SVG magic-line background** as AI Innehållsstudio.
- Removed the old review-only PNG background, fade mask and blurred colour orb that made the glass cards appear almost opaque.
- Retained the exact shared glass-card values introduced in V111.
- Extended the decorative canvas behind the review workspace so the lines remain visible through the upper and main cards.

## Deployment
- CSS-only update.
- No SQL changes.
