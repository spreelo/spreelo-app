# Deploy v144.230

Deploy normally. No SQL or environment changes are required.

After deploy:
1. Open `/grow-brain?demo=1`.
2. Keep the existing Step 3A synthetic test data in place (or rerun the Step 3A engine test).
3. Click `Run Step 4 test`.
4. A successful result should pass all safety/planning checks and show before/after simulated shares for all three goals.
