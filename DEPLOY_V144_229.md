# Deploy v144.229

1. No SQL is required if `spreelo-v144.225-SQL.sql` is already installed.
2. Deploy this build normally.
3. Keep the isolated Step 3A test data while reviewing Grow Brain if you want the 3B cards populated.
4. Real customer planning is influenced only when that customer's own performance-learning state has enough evidence. Test-brand data is isolated and cannot influence another brand.
