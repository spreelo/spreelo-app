# v144.241 — Shopify analysis-limit explanation

- Shopify onboarding now explains exactly why a brand analysis is blocked.
- Daily limit shows used/allowed counts and the reset time.
- Monthly limit shows used/allowed counts and the reset time.
- Cooldown shows the exact retry time.
- The screen explicitly confirms that Shopify is already connected and does not need reinstalling.
- Uses Spreelo translation keys; no hard-coded Swedish copy.
- No SQL migration required.
