# PATCH v144.199

## Compact personalized onboarding
- Keeps the approved v144.198 onboarding shell and planned-post carousel.
- Adds the approved desktop personalization block before the starter-plan summary:
  - Audience
  - Market
  - Offering
  - one short "Why this plan" explanation
- Adds the approved responsive mobile treatment as a compact 2x2 personalization grid.
- On mobile, channels stay directly with the plan summary and the separate long "Varied content" row is hidden to avoid duplicated explanation.
- No "Show more" link or arrow was added.
- Personalization uses existing brand-profile facts and verified product/service availability only; it does not change the plan engine or generation logic.

## i18n
- English remains the canonical source language.
- All new onboarding copy is added as `automation.onboardingV199.*` keys so existing `ui_translation_packs` can translate and persist only the missing/changed keys.
- Admin-team and admin-invitation API responses now expose stable error codes. The UI maps those codes to i18n keys instead of displaying raw backend English messages.
- Admin invitation email localization remains unchanged and continues through the existing server translation system.

## Regression tests
- Updated the stale v144.186 onboarding regression assertions to the current v144.191+ component/CSS structure.
- Updated the stale v144.191 first-run note assertion to the current v144.198 behavior.
- Added `test:v144.199` for personalized onboarding, responsive layout and admin i18n error handling.

## Preserved behavior
- Product Engine, all five product content formats, Rescue, publishing, credits, smart planning and recurring-plan pause/resume logic are unchanged.
- `johan@foldern.com` still receives the onboarding preview on every normal AI Content Studio visit.
- Customer onboarding still remains until the first real plan is successfully created.

## Database
- No SQL or database migration is required for v144.199.
