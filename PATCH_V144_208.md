# PATCH_V144_208 — Hero count alignment + planned-card text size

## Changes
- Moved the dynamic weekly post count fully inside the white panel already baked into the hero artwork.
- Rotated the number/text block by 7 degrees so it follows the same angle as the white panel.
- Tightened number/label sizing and line-height so the full value fits naturally inside the panel.
- Added responsive positioning and sizing for the same hero count on mobile.
- Reduced planned-card content-type labels below the images from 15px to 13px.
- Kept those card labels at font-weight 600.

## File changed
- `app/styles/140-v144-202-onboarding-reference-rebuild.css`
