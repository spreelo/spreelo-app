# Spreelo v144.202 — onboarding explainer responsive/reference repair

Built on v144.201 and preserves the v144.200 market-localization and typography fixes.

## What changed

- Kept every existing onboarding section and all onboarding business logic unchanged.
- Restyled only the v144.201 explainer section to match the approved desktop reference more closely:
  - the three process steps are now complete tinted cards rather than a cropped visual strip plus loose text,
  - illustrations are shown with `contain` sizing and larger available visual space so the complete artwork remains visible,
  - circular arrows overlap the gaps between the three cards,
  - benefit cards remain directly below in the same three-column rhythm.
- Replaced the mobile horizontal carousels with a true responsive vertical flow:
  - each process step becomes a full-width horizontal card with illustration on the left and text on the right,
  - arrows rotate downward between the stacked steps,
  - all three benefit cards stack full-width,
  - no explainer content requires horizontal scrolling.
- Added narrow-phone tuning down to 360 px.

## Not changed

- No SQL.
- No generation/planning/product/credit logic.
- No changes to the personalized business section, summary cards, planned-post carousel or activation buttons.
- Existing v144.201 assets and i18n keys are reused unchanged.
