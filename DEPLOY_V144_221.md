# Deploy v144.221

No SQL migration is required.

1. Deploy the app normally.
2. Open `/grow-brain?demo=1` to review Grow Brain with sample performance data.
3. Open `/grow-brain` to return to real data.

The demo mode is read-only and does not write sample data to Supabase or Grow Brain learning tables.
