# v144.222 — Grow Brain data polish

- Smooth cubic performance curve instead of straight jagged line segments.
- Trims trailing empty date buckets so missing recent measurements do not look like a performance crash to zero.
- Enlarges the chart plot area to use the performance card more effectively.
- Adds denser horizontal guides for easier reading.
- Uses stable `k` / `M` compact number formatting instead of locale-dependent abbreviations such as `tn`.
- Established learning maturity no longer renders an illogical `18 / 12`; established profiles show the analysed count with a completion mark.
- Demo top-content preview now contains six coherent lifestyle/ecommerce thumbnails rather than onboarding placeholder fragments.
- Wide desktop shows six top-content cards when space allows.

No SQL changes.
